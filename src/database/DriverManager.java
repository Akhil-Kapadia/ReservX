package database;
import java.sql.*;

public class DriverManager 
{
	public static void main(String[] args)
	{
		try(
			//Allocate a database 'Connection' object
			Connection conn = DriverManager.getConnection(
			"jdbc:mysqk://hostname:port/
			
		}
	}
}
